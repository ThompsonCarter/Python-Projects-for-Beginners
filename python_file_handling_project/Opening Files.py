
# Opening Files in Python
Before you can read from or write to a file in Python, you need to open it. Python provides a built-in open() function to access files. This function returns a file object that can be used to interact with the file.

Syntax:
```python
file_object = open('filename', 'mode')
```

- 'filename': The name of the file you want to open. This can be a file path if the file is located in a different directory.
- 'mode': The mode in which to open the file (e.g., read, write, append).
